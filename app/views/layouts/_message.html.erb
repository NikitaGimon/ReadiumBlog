<div class="row">
    <div class="col-md-12">
        <% flash.each do |name, msg| %>
            <div class="alert alert-info" role="alert">
                <p>
                    <strong>!</strong>
                    <%= msg %>
                </p>
            </div>
            <% end %>
    </div>
</div>