<div class="row">
    <div class="col-md-12">
        <% flash.each do |name, msg| %>
            <div class="alert alert-info" role="alert">

                    <strong>!</strong>
                    <%= msg %>
            </div>
            <% end %>
    </div>
</div>